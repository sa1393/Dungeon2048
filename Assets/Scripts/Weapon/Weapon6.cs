using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Weapon6 : Weapon
{
    public Weapon6(string name, int id, int attackDamage, WeaponType weaponType, int rating, string info) : base(name, id, attackDamage, weaponType, rating, info)
    {
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
